<UserControl
    x:Class="PL.Views.TaskListingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="200 5 200 0">
            <Button
                Margin="165 0 0 0"
                Padding="10 5"
                VerticalAlignment="Center"
                Command="{Binding AddNewTaskCommand}"
                Content="Add Task" />
        </StackPanel>

        <ListView
            Grid.Row="1"
            Margin="200,10,200,0"
            ItemsSource="{Binding Tasks}" SelectionChanged="ListView_SelectionChanged">
            <ListView.View>
                <GridView>

                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
